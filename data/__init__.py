"""Data-related scripts and data files for pharmaceutical-list-england.

Everything that touches the raw NHSBSA data lives here: extraction
(``extract``), transformation (``transform``), geographic enrichment
(``enrich``), and the build CLIs (``build_geo_lookup``, ``build_dataset``).
Committed data files live in ``static/`` (built artifacts), ``geo/``
(reference lookups), and ``sample/`` (offline fixtures).
"""
